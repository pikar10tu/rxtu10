# P3 เลขดาเมจ B + แบนเนอร์คัทอิน + ป้ายชื่อ + ฉาก VS — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** รีเพลย์สะใจขึ้นแบบไม่กินเครื่อง ตามแบบที่ user เลือกจากเดโม (เลข B · แบนเนอร์ 2)
สเปก §3.3–3.5 · ต่อจาก P2

- [x] **Task 11 เลขดาเมจ B** — `battleFx.js` พูล pop มีลูก ดาว/ตัวเลข/ป้าย CRITICAL สร้างครั้งเดียวใน `buildPools` · `pop()` เขียนเลขที่ `el.__n` · สีดาวตามคลาส (ปกติส้ม · crit แดง · super ส้มเข้ม · heal วงกลมเขียว · weak เทา · infect ม่วง) · ฟอนต์ Lilita One subset `+-0-9` ใน `index.html`
- [x] **Task 12 คัทอิน** — แทน `.br-spot-card` ด้วย `.br-cut` แถบเฉียง · หน้าเจ้าของจาก `defForUid(e.uid)` ทุกครั้ง · ทีมเราฟ้าเข้าซ้าย ศัตรูแดงเข้าขวา · `.br-spot` ไม่มี z-index (ม่าน z4 < การ์ดเจ้าของ z5 ≤ แถบ z5 มาทีหลังใน DOM) · จังหวะเดิม (await ใน spotlightPassive)
- [x] **Task 13 ป้ายชื่อ + ฉาก VS** — `data.sides = { top:{name,rating?}, bot:{…} }` → ป้าย `ชื่อ · แต้ม · ชื่อสนาม` · มี sides และบนไม่ใช่หอคอย = ฉาก VS 1.15 วิ แทน READY/GO · ArenaView หยิบแต้มก่อน await (CLAUDE.md ข้อ 9) · ProfileModal ส่งชื่อคนที่ท้า
- ตรวจ: build · เทส 1291 ผ่าน · harness ชั่วคราวใน dev ดูฉาก VS / คัทอิน / เลข 6 แบบ ของจริงแล้ว
