# พาสสีฟเพ็ท v2 — เพ็ทใหม่ 6 ตัว · กลไกใหม่ 11 แบบ · ตู้อัญเชิญพิเศษ

**วันที่:** 3 ก.ย. 2026 · **สถานะ:** สเปกรอ user รีวิว
**ต่อจาก:** `2026-08-27-passive-v1-design.md` · **แทนที่งานที่พักไว้:** บาลานซ์พาสสีฟ 27 ตัว (31 ส.ค.)

## 1. เป้าหมาย

1. **เพิ่มของใหม่ให้มีอะไรลุ้น** — เพ็ทใหม่ 6 ตัว (27 → 33) พร้อมตู้อัญเชิญพิเศษจำกัดเวลา
2. **ล้างพาสสีฟที่ซ้ำ/จำเจ** — ตอนนี้ 27 ตัวใช้ผลจริงแค่ ~20 แบบ (ซ้ำ 5 คู่) และหลายตัว
   ผู้เล่น **ไม่มีทางเห็นว่ามันทำงาน** (หมาป่า +4% ต่อเพื่อนสายจู่โจม อ่านบนจอได้แค่ "พลังโจมตีเพิ่ม")
3. **ทำให้การจัดทีมเป็นการตัดสินใจ** — เพิ่มตัวแก้ทาง (ล้มยักษ์ · เชื้อลุกลาม) และ
   ขั้วจัดทีมที่ขัดกันจริง: 🐺 กองสายเดียว ปะทะ 🦁 ครบ 3 สาย
4. **ไม่พังบันไดความหายาก** — ดูข้อ 8

### ไม่ทำในรอบนี้ (ตั้งใจตัด)
ระบบช่องซัพพอร์ต (เพ็ทไม่ลงสนามแต่ออร่าทำงาน) · Damage Cap · Revenge (เพื่อนล้มแล้วแรงขึ้น) ·
ตั๋วอีเวนต์แยกสกุล · เผยผลกาชาทีละใบ · ระบบหินอัพขั้นพาสสีฟ

## 2. ขอบเขต แบ่ง 5 เฟส

| เฟส | ทำอะไร | ผู้เล่นเห็นอะไร |
|---|---|---|
| **P0** ✅ | อุดบั๊กแตะพลาดปิดจอกาชา (`020deae` deploy แล้ว 3 ก.ย.) | หมุนแล้วไม่โดนกินผลอีก |
| **P1** | รื้อโครง `hook`/`effect` เดี่ยว → **`parts[]`** | ไม่เห็นอะไร — เกมต้องเหมือนเดิมเป๊ะ |
| **P2** | ลง **11 กลไกใหม่ + ขยายของเดิม 8 จุด** ในเอนจิน | ยังไม่เห็น (ยังไม่มีเพ็ทถือ) |
| **P3** | เพ็ทใหม่ 6 + เปลี่ยนกลไกเดิม 11 + เปลี่ยนชื่อ 2 + ปรับเลข 1 + อีโมจิ + ป้ายสถานะ + แจ้งผู้เล่น | เห็นครบ แต่ยังไม่เข้ากาชา |
| **P4** | บาลานซ์ด้วย sim — เลขจริงทุกตัว | ตัวเลขนิ่ง |
| **P5** | ตู้อัญเชิญพิเศษ + นับถอยหลัง + อนิเมชันไต่สี → เปิดตัว | ✨ |

🔑 **บาลานซ์ (P4) ต้องเสร็จก่อนเปิดตู้ (P5)** — ไม่งั้นคนหมุนได้ตัวใหม่ไปแล้วเราค่อยหั่นเลขทีหลัง

## 3. สถาปัตยกรรม — `parts[]` (P1)

### ปัญหาของโครงเดิม
`petPassives.js` เก็บ **`hook` 1 ค่า + `effect` 1 ค่า ต่อเพ็ท 1 ตัว** และ `effect`
ถูกใช้เป็น **กุญแจ** ทั่วระบบ: `STATUS_ICON` · `STATUS_TEXT` · `STAT_EFFECTS` ·
switch ในเอนจิน · การ์ดหน้าเพ็ท · `battleBuffs.js`

รายการใหม่มี **7 ตัวที่ต้องมีหลายผล**: 🐍 🐦‍🔥 🐘 🦣 🦍 🐱 🐢

### รูปใหม่
```js
cerberus: {
  name: 'ตรีเขี้ยวอสูร', icon: '🐕',
  parts: [ { hook: 'onAttack', effect: 'cleave', value: {...}, step: {...} } ],
  desc: '…', short: '…',
}
```
- **`parts` เป็นทางเดียว** — ไม่รองรับ `hook`/`effect` แบบเก่าคู่ขนาน (สองแหล่งความจริง = พังเงียบ)
- ทุกที่ที่เคยอ่าน `p.effect` ต้องเปลี่ยนเป็นวนทุก part · `STAT_EFFECTS` เช็คแบบ "มี part ไหนไหม"
- `passiveValueAt()` คิดขั้นแยกราย part
- **P1 ห้ามเปลี่ยนพฤติกรรมเกมแม้แต่นิดเดียว** — เทส 976 ตัวเดิมต้องผ่านครบโดยไม่แก้ค่าคาดหวัง
  (แก้ได้แค่ที่อ้าง `p.hook`/`p.effect` ตรงๆ)

## 4. กลไกใหม่ 11 แบบ (P2)

ทุกอันเป็น pure · รับ `rand` จากเอนจิน · 🔒 **ห้ามเพิ่มจำนวน beat** — แนบเป็น event `passive`
(timing ZERO) บน beat ที่มีอยู่ · `killChain` ยังเป็นข้อยกเว้นเดียว

📌 **`x%` / `y%` ในเอกสารนี้คือ "ยังไม่มีค่า" โดยตั้งใจ** — เลขจริงมาจาก sim ใน P4 ทั้งหมด
ส่วนเลขที่เขียนไว้แล้ว (🐹 200% · 🐢 20% · 🐱 2 หมัด+50% · 🦍 3% · 🐕 40% · 🦣 80% · 150% ของ
🐉/🐦‍🔥 · 👾 เพดาน 5 ชั้น · 🐭 5%) คือ **ค่าตั้งต้นที่ user เคาะมาเอง** — P2 ใช้ค่านี้ไปก่อน
แล้ว P4 เป็นคนยืนยันหรือปรับ (ถ้าปรับต้องบอก user ไม่แก้เงียบ)

| # | effect | hook | นิยาม | เพ็ท |
|---|---|---|---|---|
| 1 | `elementTrinity` | aura | ทีมมีครบ ✊✌️✋ → ทั้งทีม atk +x% และ maxHp +y% | 🦁 |
| 2 | `infect` | onAttack + damage pipeline | ดูข้อ 4.1 | 👾 |
| 3 | `taunt` | onRound | ดูข้อ 4.2 | 🦍 |
| 4 | `atkOnHit` | onHit | ทุกครั้งที่รับดาเมจ atk +x% **ไม่มีเพดาน** (user ยืนยัน) | 🦍 |
| 5 | `berserk` | onAttack | ดาเมจ +x% ต่อทุก **10% ของเลือดที่หายไป** (เลือด 40% = หาย 60% = 6 ขั้น) | 🐗 |
| 6 | `giantSlayer` | onAttack | ดาเมจ +x% ต่อทุก **10% ที่ maxHp เป้าสูงกว่า maxHp ตัวเอง** · เพดาน y% | 🦡 |
| 7 | `teamLifesteal` | aura | เพื่อนทุกตัว (รวมค้างคาว) ฟื้นเลือด x% ของดาเมจที่ **ตัวเองตีได้** | 🦇 |
| 8 | `armorStack` | onStart + onHit | ดูข้อ 4.3 | 🦣 |
| 9 | `stealStats` | setup | ขโมย atk และ maxHp อย่างละ x% จากศัตรู **ทุกตัว** — **ศัตรูเสียจริง** ไม่ใช่แค่ตัวเองได้เพิ่ม | 🐭 |
| 10 | `teamDamageReduction` | aura | ทั้งทีมรับดาเมจลด x% · **ตัวเจ้าของเป็น 2 เท่า** | 🐢 |
| 11 | `healOnAttack` | onAttack | เมื่อตี ฟื้นเลือดเพื่อนที่บอบช้ำสุด x% ของดาเมจที่ทำได้ | 🦄 |

### 4.1 `infect` — เชื้อลุกลาม (ตัวยากที่สุด)
- **แปะ:** ไวรัสตีใคร เป้านั้น +1 ชั้น · **เพดาน 5 ชั้น** · ขึ้นจากหมัดไวรัสเท่านั้น
- **ระเบิด:** เป้าที่ติดเชื้อ ถูกตีโดย**ใครก็ได้ในทีมไวรัส** (รวมไวรัสเอง) → ดาเมจเพิ่ม
  = x% ของ atk ไวรัส **× จำนวนชั้น** · เชื้อ**ไม่หาย**ตอนระเบิด
- **ทะลุทุกอย่าง:** ดาเมจเชื้อ **ไม่ผ่าน** `dodge` / `damageReduction` / `teamDamageReduction` /
  `guardian` / `armorStack` — "โรคไม่สนเกราะ" คือเหตุผลที่มันมีอยู่ ถ้าหักลดตามปกติ
  มันจะเหลือแค่ "ดาเมจเพิ่มนิดหน่อย"
- **ส่งต่อ:** ตัวติดเชื้อล้ม → โยนชั้นทั้งหมดไปให้ศัตรูตัวอื่น **สุ่ม 1 ตัว** (ยึดเพดาน 5)
- ⚠️ ไวรัสตายแล้วเชื้อที่แปะไว้**ยังทำงานต่อ** (ตั้งใจ — เพื่อนตีก็ระเบิด)
- ⚠️ สถานะนี้ต้องโผล่บนการ์ดเป็น**ตัวเลขชั้น** ไม่ใช่ป้ายเฉยๆ ไม่งั้นไม่มีใครเข้าใจว่าเกิดอะไร

### 4.2 `taunt` — ตีอกท้าชน
- ต้นรอบ: **ศัตรูทุกตัวที่ออกหมัดในรอบนั้นต้องตีมาที่กอริลลา**
- ระหว่างถูกตีจากผลบังคับ กอริลลา **ลดดาเมจที่รับ x%** (ไม่ใช่ตลอดเวลา — เฉพาะหมัดที่ถูกดึงมา)
- 🔴 **ลำดับความสำคัญของการเลือกเป้า** (ต้องเขียนเป็นเทส):
  `taunt` > `targetLowest` (🦅 กริฟฟิน) > สุ่มปกติ
- 🔴 `guardian` (🐘 รับแทน) ทำงาน**หลัง**เลือกเป้าแล้ว และ **ไม่รับแทนตัวเอง** ⇒ ถ้าเป้าคือกอริลลา บากุไม่แทรก
- ทีมเดียวมีกอริลลา 2 ตัว: **ตัวที่อยู่ช่องซ้ายสุดชนะ** (ต้อง deterministic ไม่งั้น replay ไม่ตรง)

### 4.3 `armorStack` — เกราะปฐพี (🦣 เปลี่ยนกลไก)
- เริ่มไฟต์ได้เกราะ **2 สแตค**
- เกราะกิน 1 สแตค = **กันหมัดนั้นทั้งหมด** (ไม่ใช่โล่ที่มีค่าเลือด) แล้ว **สะท้อน 80% ของดาเมจนั้น
  ใส่ศัตรูทั้งหมด**
- หมดสแตคแล้วรับดาเมจปกติ · ไม่มีการเติมระหว่างไฟต์
- ดาเมจสะท้อน **หักลดตามปกติ** (โดน dodge/DR/เกราะฝั่งตรงข้ามได้) — มีแค่ `infect` ที่ทะลุทุกอย่าง

### 4.4 ขยายของเดิม 8 จุด
| เพ็ท | เดิม | ใหม่ |
|---|---|---|
| 🐱 แมว | `cheatDeath` รอด 1 ครั้ง | รอดแล้ว **ทนต่อได้อีก 2 หมัด** และระหว่างนั้น **atk +50%** |
| 🐦‍🔥 ฟีนิกซ์ | `revive` | คืนชีพ + **ตีผู้สังหาร 150% ของ atk** (แนบบน beat การตาย) |
| 🐕 เซอร์เบอรัส | `cleave` 3 เป้าไม่ซ้ำ | **สุ่ม 3 เป้า ซ้ำตัวเดิมได้** (ตัวเดียวอาจโดน 2–3 ที) · 40% ต่อครั้ง |
| 🦖 ทีเร็กซ์ | `stackAtk` เมื่อ**ตัวเอง**ฆ่า | **ศัตรูล้มโดยใครก็ได้** → hook ใหม่ `onAnyDeath` |
| 🐺 หมาป่า | `teamAtkPerElement` (+x% ต่อเพื่อนสายจู่โจม 1 ตัว) | ออร่าตรงๆ: **เพื่อนสายจู่โจมทุกตัว atk +x%** |
| 🐍 อูโรโบรอส | `regenSelf` | `regenSelf` **+** `stackAtk` ทุกต้นรอบ (2 parts) |
| 🐘 บากุ | `guardian` | `guardian` **+** `regenSelf` (2 parts) |
| 🐉 บาฮามุท | `aoeOpener` | เลขเป็น 150% ของ atk (ค่าจริงรอ P4) |

`teamAtkPerElement` ไม่มีใครใช้แล้ว → **ลบทิ้ง** (ค่าคงที่ที่ไม่มีใครอ่าน อันตรายกว่าค่าที่ผิด)

## 5. รายการเพ็ท 33 ตัว (P3)

🆕 ใหม่ · ♻️ เปลี่ยนกลไก · ✏️ เปลี่ยนชื่อ · 💠 ออร่า (รองรับช่องซัพพอร์ตในอนาคต)
**สาย 11/11/11 · legendary 4/4/4 · epic 3/3/3** (ลงตัวทุกชั้น)

### 🌟 LEGENDARY (12)
| เพ็ท | สาย | ชื่อพาสซีฟ | กลไก |
|---|---|---|---|
| 🐉 บาฮามุท | จู่โจม | ลมหายใจราชันมังกร | เริ่มไฟต์ สาดไฟใส่ศัตรูทุกตัว 150% ของ atk |
| 👹 โอนิ | จู่โจม | อสูรจอมกระหาย | น็อกแล้วตีต่อทันที (มีเพดานต่อเทิร์น) |
| 🦖 ทีเร็กซ์ | จู่โจม | ♻️ สัญชาตญาณนักล่า | ศัตรูล้ม (ใครฆ่าก็นับ) → atk เพิ่มถาวร สแตคได้ |
| 🦁 สิงโต | จู่โจม | 🆕💠 อาณัติเจ้าป่า | ทีมครบ 3 สาย → ทั้งทีม atk + maxHp เพิ่ม |
| 🐍 อูโรโบรอส | สมดุล | ♻️ วัฏจักรไม่สิ้นสุด | ทุกต้นรอบ ฟื้นเลือดเล็กน้อย + atk สะสม |
| 🦅 กริฟฟิน | สมดุล | โฉบเด็ดชีพ | เล็งศัตรูเลือดน้อยสุดเสมอ |
| 🐦‍🔥 ฟีนิกซ์ | สมดุล | ♻️ เกิดใหม่จากเถ้า | ตายครั้งแรกฟื้นกลับ + ตีผู้สังหาร 150% |
| 👾 ไวรัส | สมดุล | 🆕 เชื้อลุกลาม | ดูข้อ 4.1 |
| 🐳 คุณวาฬ | พิทักษ์ | ✏️💠 อ้อมกอดเบลูก้า | maxHp ทั้งทีมเพิ่ม |
| 🐘 บากุ | พิทักษ์ | ✏️♻️ กลืนกินฝันร้าย | รับดาเมจแทนเพื่อนเลือดน้อยสุด + ฟื้นเลือดตัวเองทุกรอบ |
| 🦣 แมมมอธ | พิทักษ์ | ♻️ เกราะปฐพี | เกราะ 2 สแตค กันหมัดเต็ม + สะท้อน 80% ใส่ศัตรูทั้งหมด |
| 🦍 กอริลลา | พิทักษ์ | 🆕 ตีอกท้าชน | ท้าชน + ลดดาเมจจากหมัดที่ถูกดึงมา + โดนแล้ว atk +3% ไม่จำกัด |

### 💜 EPIC (9)
| เพ็ท | สาย | ชื่อพาสซีฟ | กลไก |
|---|---|---|---|
| 🐲 มังกร | จู่โจม | ลมหายใจเพลิง | เปลวไฟลามหลายตัว ตัวรองรับบางส่วน |
| 🐕 เซอร์เบอรัส | จู่โจม | ♻️ ตรีเขี้ยวอสูร | สุ่ม 3 เป้า **ซ้ำตัวเดิมได้** 40% ของ atk ต่อครั้ง |
| 🐗 หมูป่า | จู่โจม | 🆕 ยิ่งเจ็บยิ่งบ้า | เลือดยิ่งน้อย หมัดยิ่งหนัก |
| 🦄 ยูนิคอร์น | สมดุล | ♻️ เขาศักดิ์สิทธิ์ | เมื่อตี ฟื้นเลือดเพื่อนบอบช้ำสุดตามดาเมจที่ทำได้ |
| 🧚 ภูต | สมดุล | 💠 ละอองเวทมนตร์ | โอกาสคริทั้งทีมเพิ่ม |
| 🦡 แบดเจอร์ | สมดุล | 🆕 ล้มยักษ์ | ตีแรงขึ้นเมื่อเป้ามี maxHp มากกว่าตัวเอง |
| 🐼 แพนด้า | พิทักษ์ | ลมปราณฟื้นฟู | ทุกต้นรอบ ฟื้นเลือดตัวเอง |
| 🧞 จินนี่ | พิทักษ์ | ✏️ พรข้อสุดท้าย | กันเพื่อนไม่ให้ตาย 1 ครั้ง (เหลือเลือด 1) |
| 🦇 ค้างคาว | พิทักษ์ | 🆕💠 พันธะเลือด | ออร่า — ทั้งทีมฟื้นเลือดตามดาเมจที่ตัวเองตีได้ |

### 💙 RARE (6)
| เพ็ท | สาย | ชื่อพาสซีฟ | กลไก |
|---|---|---|---|
| 🐺 หมาป่า | จู่โจม | ♻️💠 สัญชาตญาณฝูง | ออร่า — เพื่อนสายจู่โจมทุกตัว atk เพิ่ม |
| 🦈 ฉลาม | จู่โจม | เขี้ยวกระหาย | ตีแรงขึ้นกับศัตรูเลือดน้อย |
| 🦊 จิ้งจอก | สมดุล | เงาลวงตา | โอกาสหลบ |
| 🐰 กระต่าย | สมดุล | ฝีเท้าสายลม | โอกาสตีสองทีรวด |
| 🦉 นกฮูก | พิทักษ์ | 💠 นัยน์ตาหยั่งรู้ | ศัตรูทุกตัวรับดาเมจเพิ่ม |
| 🦭 แมวน้ำ | พิทักษ์ | 💠 ยอดนักซัพพอร์ต | atk ทั้งทีม · คู่กับ 🐳 บัฟแรงขึ้น + ทีมฟื้นเลือดทุกรอบ (**มีอยู่แล้ว ไม่แตะ**) |

### 🤍 COMMON (6)
| เพ็ท | สาย | ชื่อพาสซีฟ | กลไก |
|---|---|---|---|
| 🦔 เม่น | จู่โจม | เกราะหนาม | สะท้อนดาเมจกลับบางส่วน |
| 🐹 แฮมสเตอร์ | จู่โจม | พลังกักตุน | เลือดเต็ม → atk **+200%** (หมัดเปิดที่แรงมาก) |
| 🐭 หนู | สมดุล | ✏️♻️ หัวขโมยตัวจิ๋ว | เริ่มไฟต์ ขโมย atk + maxHp อย่างละ 5% จากศัตรูทุกตัว |
| 🐱 แมว | สมดุล | ♻️ เก้าชีวิต | รอดตายเหลือเลือด 1 · ทนต่ออีก **2 หมัด** · ระหว่างนั้น atk +50% |
| 🦋 ผีเสื้อ | พิทักษ์ | ละอองเยียวยา | ทุกต้นรอบ ฟื้นเลือดเพื่อนบอบช้ำสุด |
| 🐢 เต่า | พิทักษ์ | ♻️ กระดองศิลา | ทั้งทีมรับดาเมจลด 20% · ตัวเต่าเอง 2 เท่า |

### งานพ่วงใน P3
- **อีโมจิ** — มีแค่ 🦁 (`1f981.svg`) · อีก 5 ตัวรัน `node scripts/fetch-fluent.mjs` (สแกนเอง)
- 🔴 **`atkStyle` ไม่ต้องตั้ง — ทุกตัวเป็น melee หมดแล้ว** `atkStyleOf()` คืน `'melee'` เสมอ
  และมีเทสล็อกไว้ (`petAtkStyle.test.js`: "แม้ data ยังมี `atkStyle:'ranged'` ค้างอยู่ ก็ต้องคืน melee")
  · สาขา ranged ใน `BattleReplay.vue:725` "หลับ" อยู่ ⇒ **ห้ามใส่ `atkStyle`/`projectile` ให้เพ็ทใหม่**
  (คอมเมนต์ในไฟล์ที่พูดถึง ranged 8 ตัว = สภาพ**เดิมก่อนเปลี่ยน** อ่านผิดง่ายมาก)
- **`desc`/`short` เป็นแม่แบบ** ห้ามพิมพ์ตัวเลขลงไปตรงๆ (กฎเหล็กเดิม) · โทนยึด `docs/voice-guide.md`
  ความยาวที่การ์ดรับได้ 40–60 ตัวอักษร
- **`step`** ตั้งให้ขั้น 3 ≈ 1.5–1.8 เท่าของขั้น 1 · ของที่โตแล้วพังใช้ `step: 0`
  (จำนวนสแตคเกราะ · ชั้นเชื้อ · จำนวนหมัดที่แมวทน)
- **ป้ายสถานะ** — การ์ดไฟต์จำกัด 3 ป้าย (วัดเดิม: เฉลี่ย 1.09 ชนเพดาน 4.7%) ของใหม่จะชนบ่อยกว่ามาก
  ⇒ วัดใหม่ด้วยทีมสุ่ม 5,000 คู่ แล้วขยายเพดาน + จัดลำดับความสำคัญ (เชื้อ/เกราะ/ท้าชน มาก่อนบัฟเล็ก)
  ทุกป้ายต้องผ่าน `utils/battleBuffs.js` ซึ่งเป็นแหล่งความจริงเดียว
- **แจ้งผู้เล่น** — 13 ตัวที่คนมีอยู่แล้วจะเปลี่ยนกลไกเอง ⇒ ประกาศผ่านกล่องข้อความ/กระดานข่าว 1 ครั้ง

## 6. ตู้อัญเชิญพิเศษ (P5)

> ## ✅ P5 ลงมือเสร็จ (11 ก.ย. 2026) — เทส 1,175 ผ่าน · build ผ่าน
>
> **ตู้ยังไม่โผล่จนกว่าแอดมินจะกด "เริ่ม 7/14 วัน"** ในการ์ดใหม่ของหน้าแอดมิน (ไม่มี `config/app.gachaEvent` = ไม่มีอีเวนต์)
>
> | ของที่ลง | ที่ไหน |
> |---|---|
> | สถานะอีเวนต์ + คลังตัวเด่น + ข้อความเวลาที่เหลือ (pure) | `src/utils/gachaEvent.js` |
> | `rollOne`/`rollMany` รับ `opts.legendaryIds` (ดีฟอลต์เหมือนเดิมเป๊ะ) | `src/utils/gacha.js` |
> | แบนเนอร์ที่ใช้ได้ทั้งสองตู้ | `src/components/shop/GachaBanner.vue` |
> | สองแบนเนอร์ซ้อนกัน (พิเศษบน · ประจำล่าง) + นาฬิกาเดินทุกวินาที | `src/views/ShopView.vue` |
> | ลูกแก้วไต่สี ขาว→ฟ้า→ม่วง→ทอง (งบเวลาเท่าเดิม 1.3 วิ) | `src/views/ShopView.vue` |
> | การ์ดแอดมิน เริ่ม 7/14 วัน · จบตอนนี้ (= ตั้ง `endsAt` เป็นตอนนี้) | `src/views/AdminView.vue` |
>
> **ที่ต่างจากแผน / เก็บเพิ่มระหว่างทาง**
> - ตัวเด่น 3 ตัวมาจากโค้ด (`EVENT_FEATURED`) ไม่ให้แอดมินพิมพ์ id เอง · คอนฟิกยังทับได้เผื่อรอบหน้า
> - **เจอบั๊กค้างจาก P3a:** `useAchievements` เรียก `releasedPets()` โดยไม่ส่งคอนฟิก ⇒ หลังอีเวนต์จบ
>   ตัวหาร "เก็บครบทุกชนิด" จะค้างที่ 27 แล้วเควสติ๊กผ่านทั้งที่ยังไม่ครบจริง — แก้แล้วในเฟสนี้
> - `pull()` กันเคสกดปุ่มตู้อีเวนต์พอดีวินาทีที่หมดเวลา (เช็ค `ev.active` ซ้ำก่อนหักเหรียญ)
> - 🔒 ตู้อีเวนต์เขียนกลับแค่ `gachaPity` — ไม่แตะ `gachaGuaranteed`/`gachaTarget` ตามกติกาข้อ 4

### ชื่อ
ตู้ปกติ **"🎰 อัญเชิญประจำ"** · ตู้อีเวนต์ **"✨ อัญเชิญพิเศษ"** พร้อมชื่อรอง **King of the Jungle**

### คอนฟิก (แก้ได้จากแอดมิน ไม่ต้อง deploy)
```js
config/app.gachaEvent = {
  name: 'King of the Jungle',
  endsAt: <timestamp>,
  featured: ['lion', 'virus', 'gorilla'],                       // legendary ที่ดันก่อน
  newPets:  ['lion','virus','gorilla','boar','badger','bat'],   // ยังไม่เข้าตู้ปกติ
}
```

### กติกา
1. **คลังในตู้อีเวนต์ = 33 ตัวเต็ม** เรตความหายากเท่าตู้ปกติเป๊ะ (45/35/16/4)
2. เมื่อออก legendary ในตู้อีเวนต์ → **ดัน `featured` ที่ยังไม่มีก่อนเสมอ** ครบ 3 แล้วตกไปตัวเก่า
   (ใช้ new-first เดิมของ `pickLegendary()` แค่ส่งรายชื่อ 3 ตัวแทนทั้งกอง)
3. **ตู้ปกติระหว่างอีเวนต์ = 27 ตัวเดิม** — `newPets` ยังไม่อยู่ในกอง
4. **ปิดระบบ "เป้าหมาย" ในตู้อีเวนต์** — ไม่งั้นเป้าที่ตั้งค้างไว้จะชนะตัวเด่น
   🔴 และ **ห้ามแตะสถานะการันตี 50/50 ของตู้ปกติ** ที่ผู้เล่นสะสมไว้
5. **pity แชร์กระเป๋าเดียว** — soft 40 / hard 50 เหมือนเดิม ไม่เพิ่มฟิลด์ใน user doc
6. **ราคาเท่าตู้ปกติ** 1,000 / 10,000 (ได้ 11) ใช้เหรียญ + ตั๋วเดิม
7. **หมดเวลาเองด้วยนาฬิกา** — `now > endsAt` ⇒ ตู้อีเวนต์หาย **และ `newPets` ไหลเข้าตู้ปกติทันที
   โดยไม่ต้องมีใครกดปุ่ม** · หน้าเว็บนับถอยหลังให้ผู้เล่นเห็น
   🔑 จงใจเลี่ยงแพทเทิร์น "⚠️ ต้องกดปุ่มแอดมิน 1 ครั้ง" ที่สะดุดมาหลายรอบ
8. โครงนี้ **ใช้ซ้ำได้** — รอบหน้าเปลี่ยนแค่ `name`/`endsAt`/`featured`/`newPets`

### อนิเมชันเผยผล
- 🔴 ของเดิมเฉลยตัวเอง: ลูกแก้วใช้ `--glow: rarityColor(reveal.best)` ตั้งแต่วินาทีแรก
  ⇒ 1.3 วิที่ควรลุ้น บอกคำตอบไปแล้ว
- ใหม่: ลูกแก้วเริ่มสีกลาง **ไต่สีทีละขั้น ขาว → ฟ้า → ม่วง → ทอง** ยิ่งสูงยิ่งเร่ง/สั่น แล้วแตกเป็นผล
- CSS + จังหวะล้วน **ไม่แตะตรรกะสุ่ม ไม่ยืดเวลารวม** · ต้องคงทาง reduced-motion เดิม (ข้ามไป `show`)

## 7. การทดสอบ

- รัน: `node --test $(find src -name "*.test.js")` — ปัจจุบัน **976 ผ่าน** ต้องผ่านครบทุกเฟส
- **P1:** ห้ามแก้ค่าคาดหวังของเทสเดิม (เปลี่ยนได้แค่ที่อ้าง `p.hook`/`p.effect`)
- **P2:** เทสต่อกลไก — โดยเฉพาะ
  - `infect`: แปะ/เพดาน/ระเบิดจากเพื่อน/ทะลุ dodge+DR+guardian+armor/ส่งต่อตอนล้ม/ไวรัสตายแล้วเชื้ออยู่
  - `taunt`: ลำดับ taunt > targetLowest > สุ่ม · กอริลลา 2 ตัวต้อง deterministic
  - `armorStack`: กันเต็มหมัด · สะท้อนใส่ทุกตัว · หมดสแตคแล้วรับปกติ
  - `stealStats`: ปรับ **ตอน setup เท่านั้น** ห้ามขยับ `maxHp` กลางไฟต์ (จะ re-compute แล้วพังทั้งไฟต์)
- **ทุกเฟส:** เทส "จำนวน beat ไม่เพิ่ม" ต้องยังผ่าน · replay ต้อง deterministic จาก seed เดิม
- **P4:** `passive-power-sim.mjs` + `passive-vs-passive-sim.mjs` (3v3 · เกรด III · ขั้น 1)

## 8. ความเสี่ยงที่รู้ตัวแล้ว

1. 🔴 **บันไดความหายากพังง่ายมาก** — `petPower.js` ตำนานแรงกว่าธรรมดาแค่ **40%**
   (atk 10→14 · hp 50→70 · จงใจ "flat กัน snowball") ⇒ **พาสสีฟคือความต่างเกือบทั้งหมด**
   และทุกคนมี common ครบจากตั๋วฟรี 50 ใบ ⇒ **ถ้า common แรงเกิน ตู้อัพเรทจะไม่มีใครหมุน**
   ⇒ กติกา P4: **เลข common ต้องต่ำกว่าคู่เทียบ epic/legendary เสมอ** ให้ sim เป็นคนเคาะ ไม่เดา
   จุดที่ต้องจับตาเป็นพิเศษ: 🐹 +200% ตอนเลือดเต็ม (รอบแรกทุกตัวเลือดเต็มเสมอ) ·
   🐢 ลดทีม 20% (ของที่ไม่มีเหตุผลจะไม่ใส่ = ล็อก 1 ใน 3 ช่องถาวร) · 🐱 ทนรวม 3 หมัด
2. 🟡 **สายสมดุลจะกลายเป็น "สายล้มแทงค์" เต็มตัว** (👾 + 🦡 ซ้อนบนธาตุที่ชนะพิทักษ์อยู่แล้ว)
   — ตั้งใจ แต่ P4 ต้องยืนยันว่าพิทักษ์ยังเล่นได้ (พิทักษ์ได้ 🦍 กับ 🦇 มาชดเชย)
3. 🟡 **ทีมบอทหอคอยชั้น 70–100 และสนามประลอง** ใช้พาสสีฟ ⇒ P4 ต้องรันเทียบก่อน/หลัง
4. 🟡 **กอริลลา atk +3% ไม่จำกัด** ในไฟต์ยาวของหอคอยชั้นสูง — user ยืนยันไม่ใส่เพดาน
   ⇒ P4 ต้องรายงานตัวเลขจริงว่าบานแค่ไหน
5. 🟡 **ป้ายสถานะล้น** — ผู้เล่นจะมองไม่เห็นว่าตัวเองติดอะไร ถ้าไม่ขยายเพดาน

## 9. คำถามที่ยังเปิดอยู่

- **การ์ดโน้ตชั่วคราวในพาแนลแอดมิน** (`AdminView.vue` `.note-card`) ยังรอสั่งลบ

### ปิดไปแล้ว
- ~~จัดโทน flavor text ของ legendary ให้เป็นชุดเดียวกับชั้นล่าง~~ **user สั่งพักไว้ (3 ก.ย.)
  "ยังไม่ใส่ ไว้วันหลัง"** — ไม่แตะ flavor ของ 27 ตัวเดิมในรอบนี้
  ✅ **ซ่อน flavor ทั้งหมดแล้ว** (`47e3230` deploy 3 ก.ย.) — จุดเดียวที่มันเคยโผล่คือกล่องข้อมูล
  ใน picker เลือกเป้าหมาย (`ShopView.vue`) ซึ่งลิสต์แค่ legendary · data ยังอยู่ครบใน `data/index.js`
  คืนได้ด้วยการเอาบรรทัดในเทมเพลต + สไตล์ที่คอมเมนต์คาไว้กลับมา
  ⇒ **เพ็ทใหม่ 6 ตัวไม่ต้องเขียน flavor เลยในรอบนี้** — ไปเขียนทั้ง 33 ตัวพร้อมกันวันที่จัดโทน

## 10. ยกไป P2 — ข้อสังเกตจากรีวิว P1

ทั้งหมดนี้ **ไม่ใช่บั๊กของ P1** (พิสูจน์แล้วว่าพฤติกรรมเกมไม่เปลี่ยน: differential 2,587 ไฟต์
เทียบกับ base ได้ผลเหมือนกันทุกไบต์) แต่เป็นข้อสมมติที่ "จริงตราบใดที่ทุกตัวมี part เดียว"
ซึ่ง **P2 คือรอบที่ทำให้มันไม่จริง** — ต้องเก็บก่อนเพิ่มเพ็ทหลาย part

1. **`partWithEffect` หาแบบ effect-first** — `battlePassives.js:240` (guardian) กับ `:315` (saveAlly)
   ดึง part แรกที่ effect ตรง *ไม่ว่าจะอยู่ hook ไหน* แล้วค่อยเช็ค hook ทีหลัง ⇒ ถ้า P2 ใส่
   `guardian` ไว้บน hook อื่นก่อนตัวจริง มันจะเจอตัวผิดแล้ว `continue` = พิทักษ์เงียบไปเฉยๆ
   สำนวนที่ปลอดภัยกว่า: `partsAt(gp, 'onHit').find(x => x.effect === 'guardian')`
2. **`partAt(p, 'onDeath')` เอา part แรกตัวเดียว** — `battlePassives.js:291` · ถ้า P2 มีตัวที่
   `revive` + `cheatDeath` บน `onDeath` พร้อมกัน จะทำงานแค่อันเดียวเงียบๆ
3. **ตัวดักซ้ำใน `battleBeats.js:160` ใช้คีย์ `` `${e.uid}:${e.effect}` ``** ⇒ เพ็ทที่มีสอง part
   ใช้ effect เดียวกัน part ที่สองจะถูกลดเป็น `skillQuiet` เงียบๆ (บั๊กตระกูลเดียวกับ CLAUDE.md ข้อ 15
   — ปลายทางไกลๆ ที่แอบสมมติรูปฟิลด์ไว้)
4. **เทสหลาย part ยังไม่ล็อกลำดับ** — สเปกบอกว่า "ลำดับใน parts = ลำดับที่ event โผล่บนจอ"
   แต่ `battlePassives.test.js:404-424` เช็คแค่ว่ามีครบ ไม่ได้เช็คลำดับ · เพิ่มบรรทัดเดียวพอ:
   `assert.deepEqual(effects, ['regenSelf', 'healLowestAlly'])`
5. **`upgradable.length >= 20`** (`battlePassives.test.js:322-334`) เปลี่ยนหน่วยไปแล้ว —
   เดิมนับ *ตัวเพ็ท* ตอนนี้นับ *part* · เท่ากันวันนี้ (27 ตัว × 1 part) แต่ P2 จะทำให้เลขพอง
6. **ออปชัน `effect` ของ `effectText`** (`petPassives.js:264-275`) รับไว้แล้วแต่ยังไม่ได้ใช้ —
   `battleBuffs.js:43` ส่งมาแล้วด้วย · P3 คือรอบที่มันจะมีความหมาย (แต่ละ part มี `short` ของตัวเอง)

## 11. ลำดับและจังหวะการออกพาสสีฟ (กติกาบังคับของ P2)

user สั่ง 3 ก.ย. 2026: **"เรียงลำดับการออกสกิลให้ดี กลัวทามมิ่งซ้อน"** — ข้างล่างคือของที่วัดจริง
แล้วแปลงเป็นกฎ ไม่ใช่ความรู้สึก

### วัดจากของจริงก่อน (400 ไฟต์ ทีมสุ่มจากเพ็ท 27 ตัวที่มีพาสสีฟ ช่องละ 3 ตัว)

| วัดอะไร | ได้เท่าไร |
|---|---|
| event พาสสีฟทั้งหมด | **20.57 ครั้ง/ไฟต์** |
| ได้หยุดจริง (`skill` 200ms) | 3.55 ครั้ง/ไฟต์ |
| เงียบสนิท (`skillQuiet` 0ms) | **15.43 ครั้ง/ไฟต์** |
| โมเมนต์เต็ม (`skillMoment` 1,040ms) | 0.53 ครั้ง/ไฟต์ |
| ก้อนเปิดไฟต์ (`openGroup` 1,100ms) | 0.71 ครั้ง/ไฟต์ |
| เวลาที่พาสสีฟกินไป | **2,039 ms จาก 20,032 ms = 10.2% ของไฟต์** |
| ป้ายที่โผล่ติดกันโดยไม่มีอะไรคั่น | สูงสุด **5 ใบ** · 3 ใบขึ้นไป ~1.00 ครั้ง/ไฟต์ · 2 ใบ 2.92 ครั้ง/ไฟต์ |

⇒ **ทามมิ่งวันนี้ยังไม่ซ้อน** เพราะทุกตัวมี part เดียว และ 75% ของ event ถูกปิดเสียงอยู่แล้ว
แต่ตัวเลข "5 ใบติดกัน" คือเพดานที่ P2 จะดันขึ้นทันทีที่เพ็ทตัวเดียวยิงหลาย part

### กฎ 4 ข้อที่ P2 ต้องทำตาม

1. **หนึ่งเพ็ท × หนึ่ง hook = หนึ่งจังหวะ ไม่ว่าจะมีกี่ part** — ยิง event ตามลำดับใน `parts[]`
   แต่ให้ **ใบสุดท้ายของก้อนเท่านั้นที่ถือเวลาหยุด** ที่เหลือ 0ms
   (กลไกนี้มีอยู่แล้วใน `battleBeats.js` ชื่อ `openQuiet`/`openGroup` ที่ใช้กับยกแรก — ทำซ้ำแบบเดียวกัน
   อย่าเพิ่มชั้นเวลาใหม่) · ถ้าปล่อยตามโค้ดวันนี้ เพ็ท 3 part ที่ effect ไม่ซ้ำกันจะได้
   `SKILL_PAUSE` 200ms × 3 = **หยุด 600ms ติดกันในรอบเดียว**
2. **ตัวดักซ้ำต้องเลิกใช้คีย์ `` `${e.uid}:${e.effect}` ``** (`battleBeats.js:160`) — เพ็ทที่มีสอง part
   ใช้ effect เดียวกัน part ที่สองจะโดนลดเป็น `skillQuiet` เงียบหายไปเลย · ใส่ลำดับ part
   หรือ `tag` ลงในคีย์ด้วย
3. **ลำดับใน `parts[]` = ลำดับที่เห็นบนจอ** — เป็นสัญญาที่เขียนไว้ในสเปกแล้วแต่ยังไม่มีเทสล็อก
   P2 ต้องเพิ่ม `assert.deepEqual(effects, [...])` ในเทสเพ็ทหลาย part (ดู §10 ข้อ 4)
4. **เพดานป้ายต่อการ์ด `STATUS_MAX = 3`** วัดไว้ตอนทุกตัวมี part เดียว: เฉลี่ย 1.09 ใบ ชนเพดานแค่ 4.7%
   P2 ต้อง **วัดใหม่** หลังเพิ่มเพ็ทหลาย part — ถ้าชนเพดานบ่อยขึ้นมาก ผู้เล่นจะมองไม่เห็นว่าตัวเองติดอะไร

## 12. กติกาการเขียนคำอธิบายสกิล (บังคับตั้งแต่ P2 เป็นต้นไป)

user สั่ง 3 ก.ย. 2026: **"อธิบายตรงๆ พร้อมแจ้งตัวเลขเลย · ไม่เอาคำไม่ทางการ"**

1. **แจ้งตัวเลขทุกตัวที่ข้อมูลมี** — ทุกคีย์ใน `value` ที่เป็นตัวเลขต้องถูกพูดถึงใน `desc`
   ผ่านช่อง `{...}` · ค่าที่ไม่ถูกพูดถึง = ผู้เล่นไม่มีทางรู้ว่ามีอยู่
2. **ห้ามพิมพ์ตัวเลขที่จูนได้ลงไปตรงๆ** — พิมพ์ได้เฉพาะเลขที่ตายตัวอยู่ในโค้ด
   (เช่น "เหลือเลือด 1", "ล้มศัตรู 1 ตัว") · ที่เหลือให้ `passiveText()` เติมจาก `value`/`step`
3. **ห้ามใช้คำย่อ/คำไม่ทางการ** — "คริ" ต้องเป็น "คริติคอล" · "เทิร์น" ต้องเป็น "รอบ"
   (ทั้งเกมใช้คำว่า "รอบ" ที่อื่นหมดอยู่แล้ว)
4. **เขียนให้อ่านได้ทางเดียว** — เลี่ยงคำที่แยกวรรคผิดแล้วความหมายเปลี่ยน
   (ของเดิม "ตัวรองรับ 50% ของดาเมจ" อ่านได้ว่า "ตัว-รองรับ" → แก้เป็น "ตัวรองโดน 50%")
5. **`short` กับ `STATUS_TEXT` ของคนละกลไกต้องอ่านไม่เหมือนกัน** — ฟีนิกซ์ (ฟื้นคืนชีพ 35%)
   กับแมว (รอดด้วยเลือด 1) เคยอ่านได้ว่า "กันตายได้ 1 ครั้ง" เหมือนกันเป๊ะทั้งคู่

**เทสบังคับกฎ 1–3 อยู่ใน `src/data/petPassives.test.js` แล้ว** (3 เคส) ⇒ เพ็ทใหม่ของ P2/P3
ที่เขียนคำอธิบายผิดกติกาจะเทสแดงทันที ไม่ต้องมานั่งไล่ตรวจด้วยตา

### ⏸ รอทำพร้อม "ดันเจี้ยนหินอัพขั้นสกิล" (user เคาะเลื่อน 3 ก.ย. 2026)

`passiveText()` ถูกเรียกโดย **ไม่ส่งขั้น** อยู่ 3 จุด ⇒ โชว์เลขของขั้น 1 เสมอ

- `src/components/battle/BattleReplay.vue:161` (การ์ด inspect กลางไฟต์)
- `src/components/battle/BattleReplay.vue:489` (`passiveDescOf` ตอนประกาศสกิล)
- `src/components/pets/PetDetailModal.vue:26` (หน้ารายละเอียดเพ็ท)

**วันนี้ยังไม่ผิด** เพราะไม่มีอะไรในเกมเซ็ต `passiveLv` เลย (ทุกตัวเป็นขั้น 1 หมด) —
`PetScoutCard.vue:24` เป็นจุดเดียวที่ส่งขั้นถูกอยู่แล้ว (`passiveText(passive, pet.passiveLv)`)
⇒ **วันที่ระบบหินอัพขั้นมา ต้องแก้ 3 จุดนี้พร้อมกัน** ไม่งั้นคำอธิบายจะโกหกผู้เล่นที่อัพขั้นแล้วทันที
(เอนจินคิดเลขถูกตามขั้นอยู่แล้วที่ `battlePassives.js:14` — โกหกเฉพาะข้อความบนจอ)

⚠️ ยังค้าง (ต้องต่อท่อก่อนถึงจะแจ้งเลขได้): `STATUS_TEXT` เป็นข้อความคงที่ เติมค่าไม่ได้
   ⇒ ป้าย `duoRegen` บอกได้แค่ "ทีมฟื้นเลือดทุกรอบ" ไม่ได้บอกว่า 3%/รอบ · ป้าย `revive`/`cheatDeath`
   ต้องพิมพ์ "1 ครั้ง" ตายตัว · ถ้า P3 จะให้ป้ายบอกเลขด้วย ต้องทำให้ `STATUS_TEXT` รับค่าเหมือน `short`

## ภาคผนวก — บันทึกการตัดสินใจ

| เรื่อง | เคาะว่า | เหตุผล |
|---|---|---|
| ของใหม่ไปอยู่ไหน | เพ็ทใหม่ 6 + เปลี่ยนของเดิม | ได้ของใหม่ให้ลุ้น และล้างของจำเจไปพร้อมกัน |
| Damage Cap | **ตัดทิ้ง** | atk ทุกตัวจูนใกล้กัน เพดานต่อหมัดแทบไม่มีวันทำงาน |
| Revenge | เก็บเป็นไอเดียเพ็ทรอบหน้า | 🐕 เซอร์เบอรัสคงกลไกเดิม (การตี ≠ battlecry) |
| ตู้อัพเรท | ตู้จำกัดเวลาจริง | เกมมีระบบ "เป้าหมาย" อยู่แล้ว ⇒ อัพเรทแบบเพิ่ม % จะไม่รู้สึกอะไร |
| คลังตู้อีเวนต์ | 33 ตัวเต็ม ดันตัวเด่นก่อน | เฉพาะตัวใหม่ล้วน = 10 ครั้งครบ ของหมดเสน่ห์วันแรก |
| pity | แชร์กระเป๋าเดียว | แยกแล้วตู้ปิด = ตัวนับหายทั้งดุ้น มีแต่โทษ |
| ปิดตู้ | นาฬิกาใน config | เลี่ยง "ลืมกดปุ่มแอดมิน" ที่สะดุดมาหลายรอบ |
| ชื่อตู้ | อัญเชิญประจำ / อัญเชิญพิเศษ | คงคำที่ผู้เล่นคุ้นแล้ว เติมแค่คำขยาย |
| อนิเมชัน | ไต่สีลูกแก้ว | ของเดิมเฉลยคำตอบตั้งแต่วินาทีแรก |
